services:
  open-stream-m3u:
    build: .
    ports:
      - "7001:7001"
    environment:
      - CONFIG_SECRET=${CONFIG_SECRET:-change-me-in-production}
      - DEBUG=true
      - CACHE_TTL=${CACHE_TTL:-6h}
      - MAX_CACHE_ENTRIES=${MAX_CACHE_ENTRIES:-500}
    restart: unless-stopped
